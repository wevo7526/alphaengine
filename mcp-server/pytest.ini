[pytest]
# Run from mcp-server/. pythonpath=. lets tests import quant_core without an
# install step. Golden tests must stay green on the pinned numeric stack.
pythonpath = .
testpaths = tests
addopts = -q
