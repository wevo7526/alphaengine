# AlphaEngine gateway (mcp-server) — pinned for determinism on the algo plane.
#
# The numeric stack is pinned EXACTLY to the versions the golden-output fixtures
# were frozen against (see docs/INVENTORY.md §5). A minor bump can change a
# regression tail and therefore a signal — CI runs the golden tests to catch
# drift. Do not loosen these without re-freezing the fixtures.

# --- numeric core (EXACT pins — determinism contract) ---
numpy==2.2.6
scipy==1.15.3
statsmodels==0.14.6
scikit-learn==1.7.2
pandas==2.3.3
hmmlearn==0.3.3

# --- gateway / surfaces ---
pydantic==2.13.4
fastapi==0.136.3
uvicorn[standard]==0.48.0
httpx==0.28.1
# mcp (FastMCP) pinned when server.py lands (T8)

# --- tests ---
pytest>=8,<9
