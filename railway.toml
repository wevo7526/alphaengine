[build]
# Railway auto-detects Dockerfiles in subdirectories
# Deploy backend and frontend as separate services in the same project

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
